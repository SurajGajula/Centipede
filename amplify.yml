version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "No build process needed for static site"
    build:
      commands:
        - echo "Files are already in the correct location"
        - ls -la
  artifacts:
    baseDirectory: .
    files:
      - '**/*'
  cache:
    paths: [] 